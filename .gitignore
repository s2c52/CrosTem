.DS_Store
node_modules/
dist/
*.zip
.worktrees/
.backups/
e2e-results/
store-assets/.profile/
crostem-v*.zip

# Internal development files (kept local, not published)
.higinio/
HANDOFF.md
HANDOFF-archive.md
ROADMAP.md
docs/store-checklist.md
coverage/

# Local debug artifacts (use a ".local." infix so they never get committed)
*.local.*
# Root-level throwaway screenshots (real icons live under public/icons & store-assets)
/*.png

# Secrets — never commit
.env
.env.*
*.pem
*.key
*.p12
.npmrc
